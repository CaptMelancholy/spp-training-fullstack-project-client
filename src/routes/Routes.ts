const DefaultRoutes = {
  default: '/',
  home: '/home',
  authorization: '/auth',
  registration: '/reg',
  success: '/success',
  error: '/error',
  logout: '/logout',
};

export default DefaultRoutes;
